services:
  - type: web
    name: hyggeblog
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python seed.py; gunicorn app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.12"
